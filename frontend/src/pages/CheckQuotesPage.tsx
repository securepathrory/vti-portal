import { Box, Typography } from '@mui/material';

function CheckQuotesPage() {
    return (
        <Box sx={{ maxWidth: 600, mx: 'auto', mt: 4 }}>
            <Typography variant="h4" gutterBottom sx={{ color: '#005670' }}>
                Check Quotes
            </Typography>
            <Typography>Placeholder for viewing quotes.</Typography>
        </Box>
    );
}

export default CheckQuotesPage;